# QA Review Package

Task ID: P0-01
Goal: Tighten the first service/store refactor based on blind review findings by verifying the facade directly, separating template service from workflow service, and isolating mutable runtime data from committed template seed data.
Commit: 01630c9

Changed files:

- `config/default.json`
- `src/main/api.js`
- `src/main/services/templateService.js`
- `src/main/services/workflowService.js`
- `src/main/stores/pathStore.js`
- `src/main/stores/templateStore.js`
- `src/main/stores/workflowStore.js`
- `qa/verify-p0-01.js`

Expected behavior:

- `src/main/api.js` remains a facade only and is directly exercised by verification.
- Workflow and template concerns are separated at the service layer.
- Committed template seed data remains under `data/rongyu/templates`.
- Mutable runtime artifacts are written under `data/rongyu/runtime`.
- The service/store path still supports workflow save/load and default template resolution.

Local verification performed:

- `node qa\\verify-p0-01.js`
  - Result: passed.
  - Verified `api.js` façade functions, workflow save/load through the façade, template listing, default template resolution, and runtime write location.
- `Get-ChildItem -Recurse data\\rongyu | Select-Object FullName,Mode`
  - Result: runtime and template roots are separated under `data/rongyu/`.

Known risks:

- Persistence remains partial; run, prefab, and schema stores are still pending.
- Verification is still script-based smoke coverage, not yet wired into Vitest.
- `api.js` naming still reflects legacy structure even though its behavior is now closer to an application facade.

Reviewer mode: separate session preferred
Review result: pending
Follow-up fixes: pending review
