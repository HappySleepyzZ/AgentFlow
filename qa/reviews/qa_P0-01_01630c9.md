# QA Review Package

Task ID: P0-01
Goal: Tighten the first service/store refactor by verifying the facade directly, separating template service from workflow service, isolating mutable runtime data from committed template seed data, and closing the final path and packaging gaps found in second-pass review.
Commit: 01630c9

Changed files:

- `config/default.json`
- `src/main/api.js`
- `src/main/services/templateService.js`
- `src/main/services/workflowService.js`
- `src/main/stores/pathStore.js`
- `src/main/stores/templateStore.js`
- `src/main/stores/workflowStore.js`
- `package.json`
- `qa/verify-p0-01.js`

Expected behavior:

- `src/main/api.js` remains a facade only and is directly exercised by verification.
- Workflow and template concerns are separated at the service layer.
- Committed template seed data remains under `data/rongyu/templates`.
- Mutable runtime artifacts are written under `data/rongyu/runtime`.
- Absolute configured path overrides remain absolute instead of being forced under the repo root.
- Packaged builds include the committed template seed directory used by the runtime path contract.
- The service/store path still supports workflow save/load and default template resolution.

Local verification performed:

- `node qa\\verify-p0-01.js`
  - Result: passed.
  - Verified all facade entrypoints used by the task.
  - Asserted `src/main/api.js` does not import `fs`, `path`, `workflowStore`, or `templateStore`.
  - Asserted `workflowService` depends only on `workflowStore`.
  - Asserted `templateService` depends only on `templateStore`.
  - Asserted `pathStore` resolves configured roots through the same shared contract used in production.
  - Asserted absolute configured roots remain absolute.
  - Verified workflow save/load through the facade.
  - Verified default template resolution and template listing through the facade.
  - Verified mutating the returned default template object in memory does not affect a subsequent template load.
  - Verified saving a workflow derived from the default template does not mutate the committed template seed file.
  - Verified packaged build configuration includes `data/rongyu/templates/**/*`.
- `npm run qa:p0-01`
  - Result: passed.
  - Confirms the same verification is runnable through a package script rather than an ad hoc command.

Second-pass review:

- Reviewer type: session
- Result: pass_with_notes
- Command: `codex-paperhub exec review --uncommitted --ephemeral`
- Summary: The independent CLI review reported no blocking regression in the current changes and confirmed the path-contract and packaged-template fixes are consistent with the updated implementation.
- Findings fixed:
  - Fixed packaged build asset drift by bundling `data/rongyu/templates/**/*`.
  - Fixed `pathStore` so absolute configured roots remain absolute while repo-relative defaults still resolve from the app root.
  - Strengthened `qa/verify-p0-01.js` to validate the shared path resolution contract and packaged template seed path.

Known risks:

- Persistence remains partial; run, prefab, and schema stores are still pending.
- Verification is script-based smoke coverage and not yet migrated into Vitest.
- `api.js` naming still reflects legacy structure even though its behavior is now closer to an application facade.
