# QA Review Package

Task ID: P0-01
Goal: Introduce `services/` and `stores/` boundaries in `src/main` and move workflow/template file access out of the API facade.
Commit: 037d5a0

Changed files:

- `config/default.json`
- `docs/EXECUTION_PLAN.md`
- `src/main/api.js`
- `src/main/services/workflowService.js`
- `src/main/stores/pathStore.js`
- `src/main/stores/templateStore.js`
- `src/main/stores/workflowStore.js`
- `data/rongyu/templates/hello_world.json`

Expected behavior:

- `src/main/api.js` should act as a facade only, with no direct filesystem access.
- Workflow file access should flow through `services/` and `stores/`.
- Template file access should flow through `services/` and `stores/`.
- Development data root should resolve through config and point to `data/rongyu`.
- Default template lookup should succeed through the new store/service path.

Local verification performed:

- `node -e "const service=require('./src/main/services/workflowService'); const workflow={id:'qa_store_smoke',name:'QA Store Smoke',nodes:[],edges:[]}; service.saveWorkflow(workflow); const loaded=service.loadWorkflow('qa_store_smoke'); console.log(JSON.stringify(loaded));"`
  - Result: workflow saved and loaded successfully through service/store path.
- `node -e "const service=require('./src/main/services/workflowService'); console.log(JSON.stringify({templates:service.getAllTemplates(), defaultTemplate:service.getDefaultTemplate().name}, null, 2));"`
  - Result: template listing returned `hello_world`; default template resolved to `Hello World`.
- `Get-ChildItem -Recurse data\\rongyu | Select-Object FullName,Mode`
  - Result: `data/rongyu/templates` and `data/rongyu/workflows` were created as expected.

Known risks:

- Persistence abstraction is still minimal; run, prefab, and schema stores are not implemented yet.
- `api.js` is still named as an API module even though it now behaves like a facade.
- No automated test file was added yet; verification is currently command-based smoke testing.

Reviewer mode: separate session preferred
Review result: pending
Follow-up fixes: pending review
